import { LoadjsDirective } from './loadjs.directive';

describe('LoadjsDirective', () => {
  it('should create an instance', () => {
    const directive = new LoadjsDirective();
    expect(directive).toBeTruthy();
  });
});
